{
    "bootstrap.servers": <replace with the host of your cluster bootstrap>,
    "security.protocol": "SASL_SSL",
    "sasl.mechanisms":"SCRAM-SHA-512",
    "sasl.username":"test",
    "sasl.password":<replace with the user password>,
    "ssl.ca.location":<replace with the location of the ca.crt file>,
    "api.version.request": true,
    "broker.version.fallback": "0.10.4.0",
    "log.connection.close" : false,
    "group.id": "test-starter-consumer"

}